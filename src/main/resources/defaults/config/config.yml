#Should the server use a custom resource pack
#This is used by items and blocks with custom textures so if you want to use those this should be true
resourcepack: true

#This is the name of the file generated by the plugin that is the resource pack
#Make sure the name of the file ends in .zip
resourcepack_name: pack.zip